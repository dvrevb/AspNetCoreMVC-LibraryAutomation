@model IEnumerable<KutuphaneOtomasyonu.Models.Tur>

@{
    ViewData["Title"] = "Tür";
}

<h1>Tür</h1>

<p>
    <a asp-action="Create">Yeni Oluştur</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TurAd)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TurAd)
            </td>
            <td>
                <form asp-action="Edit" method="get" style="display:inline-block">
                    <input type="hidden" name="Id" value="@item.Id" />
                    <button type="submit" class="btn fas fa-edit " style="float:right" title="Tür Düzenle" />
                </form>
                <form asp-action="Delete" method="post" style="display:inline-block">
                    <input type="hidden" name="Id" value="@item.Id" />
                    <button type="submit" class="btn fas fa-trash " style="float:right" title="Tür Sil" />
                </form>
            </td>
        </tr>
}
    </tbody>
</table>
