@model IEnumerable<KutuphaneOtomasyonu.Models.Yayinevi>

@{
    ViewData["Title"] = "Yayınevleri";
}

<h1>Yayınevleri</h1>

<p>
    <a asp-action="Create">Yeni Oluştur</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ad)
            </td>
            <td>
                <form asp-action="Edit" method="get" style="display:inline-block">
                    <input type="hidden" name="Id" value="@item.Id" />
                    <button type="submit" class="btn fas fa-edit " style="float:right" title="Yayınevi Düzenle" />
                </form>
                <form asp-action="Delete" method="post" style="display:inline-block">
                    <input type="hidden" name="Id" value="@item.Id" />
                    <button type="submit" class="btn fas fa-trash " style="float:right" title="Yayınevi Sil" />
                </form>
            </td>
        </tr>
}
    </tbody>
</table>
